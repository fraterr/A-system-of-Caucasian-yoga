{
  "name": "caucasian-yoga-site",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "sync": "node scripts/sync-pages.mjs",
    "dev": "npm run sync && astro dev",
    "build": "npm run sync && astro build && pagefind --site dist",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.13.0",
    "marked": "^15.0.12"
  },
  "devDependencies": {
    "pagefind": "^1.3.0"
  }
}